---
title: "Create Nodes and Edges"
sidebarTitle: "Create Nodes and Edges"
description: "Once you have a valid change, you can start adding new nodes and edges to it"
---

<Tabs>
  <Tab title="cURL">
    - Request

    ```bash
    # Create nodes Jane and John + edge 'Jane knows John'
    curl "https://engines.turingdb.ai/sdk/query?graph=$GRAPH&change=$CHANGE" \
         -H "Authorization: Bearer $MY_API_KEY" \
         -H "Turing-Instance-Id: $MY_INSTANCE_ID" \
         -H "Accept: application/json" \
         -d 'CREATE (n:Person {Name:"Jane"})-[e:knows]-(m:Person {Name:"John"})'
    ```

    - Response
      TO ADD
      - Request

    ```bash
    curl "https://engines.turingdb.ai/sdk/query?graph=$GRAPH&change=$CHANGE" \
         -H "Authorization: Bearer $MY_API_KEY" \
         -H "Turing-Instance-Id: $MY_INSTANCE_ID" \
         -H "Accept: application/json" \
         -d 'COMMIT'
    ```

    - Response

    ```json
    {
      "header": {
        "column_names": [],
        "column_types": []
      },
      "data": [],
      "time": 0.070778
    }
    ```

    ### Then, commit them
  </Tab>
  <Tab title="Javascript">
    - Request

    ```jsx
    // Create nodes Jane and John + edge 'Jane knows John'
    fetch(
    	`https://engines.turingdb.ai/sdk/query?graph=${GRAPH}&change=${CHANGE}`,
    	{
    		method: "POST",
    		headers: {
    			Authorization: `Bearer ${MY_API_KEY}`,
    			"Turing-Instance-Id": `${MY_INSTANCE_ID}`,
    			Accept: "application/json",
    		},
    		body: 'CREATE (n:Person {Name:"Jane"})-[e:knows]-(m:Person {Name:"John"})',
    	},
    );
    ```

    - Response

    ```json
    {
      "header": {
        "column_names": [],
        "column_types": []
      },
      "data": [],
      "time": 0.267249
    }
    
    {
    }
    ```

    ### Then, commit them

    - Request

    ```jsx
    // Create nodes Jane and John + edge 'Jane knows John'
    fetch(
    	`https://engines.turingdb.ai/sdk/query?graph=${GRAPH}&change=${CHANGE}`,
    	{
    		method: "POST",
    		headers: {
    			Authorization: `Bearer ${MY_API_KEY}`,
    			"Turing-Instance-Id": `${MY_INSTANCE_ID}`,
    			Accept: "application/json",
    		},
    		body: 'COMMIT',
    	},
    );
    ```

    - Response

    ```json
    {
      "header": {
        "column_names": [],
        "column_types": []
      },
      "data": [],
      "time": 0.070778
    }
    ```
  </Tab>
  <Tab title="Python SDK">
    COMING
  </Tab>
</Tabs>